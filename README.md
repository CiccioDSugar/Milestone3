MILESTONE3

Projects:
  Minishell: Implemented a base bash in C with:
              - 7 Built-in commands (cd, pwd, echo, env, export, unset, exit). 
              - Redirections and heredoc.
              - Pipe (|) sistem. 

  philo(Philosophers): Solving the problem "of the philosophers at the table" in C.
              - Multithread with dataraces and race-codntions debbugging.
              - Avoid datarace and race-conditions by using mutex.
              
